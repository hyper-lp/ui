# Database
DATABASE_URL="postgresql://user:password@localhost:5432/mydb"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=""

# Privy (Client-side)
NEXT_PUBLIC_PRIVY_APP_ID=""

# Privy (Server-side)
PRIVY_APP_SECRET="" # Your Privy App Secret from dashboard

# API Security
API_SECRET_KEY="" # Generate with: openssl rand -hex 32
RATE_LIMIT_ENABLED="true" # Set to false for development

# Inngest
INGEST_CLIENT_ID="hyperlp"
INGEST_BRANCH="main"

# HyperCore Configuration (for hedge positions)
HYPERCORE_RPC_URL="https://api.hyperliquid.xyz" # HyperCore API endpoint

# Monitored Wallets (comma-separated EOA addresses)
# These wallets will be monitored for HYPE/USDT0 positions across all DEXs
MONITORED_WALLETS="" # Example: "0xWallet1,0xWallet2,0xWallet3"

# Rebalance Configuration
REBALANCE_THRESHOLD="0.05" # Delta drift threshold (5% default)
REBALANCE_CHECK_INTERVAL="*/5 * * * *" # Check every 5 minutes

# Analytics Configuration
ANALYTICS_CRON="*/15 * * * *" # Run every 15 minutes
STORE_ANALYTICS="true" # Set to true to store analytics in database
ANALYTICS_RETENTION_DAYS="30" # Number of days to keep analytics snapshots

# Legacy LP Positions Configuration (deprecated - use MONITORED_WALLETS instead)
LP_POSITIONS=""